:: Open an interactive bash shell inside the container

::docker run --rm -it docker:latest sh
docker run --rm -it docker:dind sh
::docker run --rm -it docker:cli sh

:: Command to check if docker daemon is running:
:: docker info
